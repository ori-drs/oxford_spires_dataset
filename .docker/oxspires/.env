UID=1000 # Use your own User ID
GID=1000 # Use your own Group ID
USERNAME=docker_dev # Set your username for the container

DOCKER_HOME_DIR=/home/${USERNAME} # Home directory inside the container
DOCKER_WORKDIR=${DOCKER_HOME_DIR}/oxford_spires_dataset # Path to the main codebase inside the docker container